services:
    postgres:
        image: "postgres:alpine3.20"
        environment:
            - "POSTGRES_DB=reminder"
            - "POSTGRES_PASSWORD=password"
            - "POSTGRES_USER=thomas"
        ports:
            - "5432:5432"
